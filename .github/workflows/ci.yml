# .github/workflows/ci.yml
name: ci

on: [push, pull_request]

jobs:
  test:
    uses: catalyst/catalyst-moodle-workflows/.github/workflows/ci.yml@main
    secrets: inherit
    with:
      disable_behat: true
      disable_phpdoc: true
      disable_phpcpd: true
      enable_composer_release: true
